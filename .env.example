# Supabase — add your project credentials to connect to a persistent database.
# Copy this file to .env.local and fill in the values.
# Leave blank to run with in-memory storage (data resets on every server restart).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
